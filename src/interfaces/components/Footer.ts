
import {IconType} from 'react-icons';
export interface IconsFooterProps {
    numberPhone: string
    address: string
    privacy: string
    copy: string
    phone: IconType
    marker: IconType
    fb:IconType
    in:IconType
    insta:IconType
}